import React from 'react';

// This component is now just a placeholder since the functionality
// is handled by UserProfileNavbarItem
const UserProfile: React.FC = () => {
  return null;
};

export default UserProfile;